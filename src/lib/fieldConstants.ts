/**
 * Normalised horizontal position of the line of scrimmage (0–1).
 * Shared by the formation view and the coverage view so anything
 * that needs to mirror around the LOS (offense in coverage view)
 * stays in sync with where it's drawn.
 */
export const LOS_X = 0.62;